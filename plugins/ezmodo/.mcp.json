{
  "mcpServers": {
    "ezmodo": {
      "command": "npx",
      "args": ["-y", "-p", "@ezmodo/mcp-server@0.14.2", "ezmodo-mcp-server"],
      "env": {
        "EZMODO_API_KEY": "${EZMODO_API_KEY:-}",
        "EZMODO_API_URL": "${EZMODO_API_URL:-}"
      }
    }
  }
}
