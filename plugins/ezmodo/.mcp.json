{
  "mcpServers": {
    "ezmodo": {
      "command": "npx",
      "args": ["-y", "-p", "@ezmodo/mcp-server@0.18.0", "ezmodo-mcp-server"],
      "env": {
        "EZMODO_API_KEY": "${EZMODO_API_KEY:-}",
        "EZMODO_API_URL": "${EZMODO_API_URL:-}"
      }
    }
  }
}
